package com.company;
/*
This class represents a regular path query as an object. its constructor would need to accept a regular path query, however
we decide to represent that.
It would then turn that query into an automaton that can be fed into a graph
 */

public class PathQuery {
}
