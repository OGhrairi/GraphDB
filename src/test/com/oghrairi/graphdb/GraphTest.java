package com.oghrairi.graphdb;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.Vector;

import static org.junit.jupiter.api.Assertions.*;

/*
class GraphTest {
    String vl1 = "vertex 1";
    String vl2 = "vertex 2";
    String el1 = "edge 1";
    String graphName = "graph 1";
    Graph g;
    @Test
    void addObjects(){
        g = new Graph(graphName);
        assertTrue(g.getName() == graphName, "was the graph created?");
        g.addVertex(vl1);
        assertTrue(g.vertexCount()[0] == 1,"was vertex 1 added to id list?" );
        assertTrue(g.vertexCount()[1] == 1,"was vertex 1 added to vertex list?");
        g.addVertex(vl2);
        assertTrue(g.vertexCount()[0] == 2,"was vertex 2 added to id list?");
        assertTrue(g.vertexCount()[1] == 2,"was vertex 2 added to vertex list?");
        g.addEdge(el1,0,1);
        assertTrue(g.edgeCount()[0] == 1,"was edge 1 added to id list?");
        assertTrue(g.edgeCount()[1] == 1,"was edge 1 added to edge list?");
        String out = g.getGraph();
        System.out.println(out);
        String desired = "Vertices: 0 1\n Edges: 0";
        assertEquals(out,desired);
    }



}

*/